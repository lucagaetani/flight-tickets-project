import React from 'react'
import {
    Container
} from "@mui/material";

const RegisterPageWrite = () => {
    return (
        <Container sx={{
            fontSize: "30px", 
            fontWeight: "bold",
            fontFamily: "sans-serif",
            textAlign: "center"
        }}>
            Register
        </Container>
    );
};

export default RegisterPageWrite;